import XCTest
@testable import ViewControllerAdapter

final class ViewControllerAdapterTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(ViewControllerAdapter().text, "Hello, World!")
    }
}
